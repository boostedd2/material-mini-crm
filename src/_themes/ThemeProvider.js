import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4CAF50',
      light: '#E8F5E9',
      darkHeading: '#424242',
    },
    secondary: {
      main: '#ffffff',
    },
    errorLighter: {
      main: '#FFEBEE'
    },
    lightBackground: {
      main: '#f1f1f1'
    }
  },
});
