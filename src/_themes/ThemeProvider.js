import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4CAF50',
    },
    secondary: {
      main: '#ffffff',
    },
    lightBackground: {
      main: '#f1f1f1'
    }
  },
});
